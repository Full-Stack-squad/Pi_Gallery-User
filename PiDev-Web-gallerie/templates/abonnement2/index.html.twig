{% extends 'templateBack.html.twig' %}

{% block title %}Mes abonnements{% endblock %}

{% block body %}
    <h1>Les abonnements</h1>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Anom</th>
                <th>Idu</th>
                <th>Ida</th>
                <th>Noma</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for abonnement2 in abonnement2s %}
            <tr>
                <td>{{ abonnement2.id }}</td>
                <td>{{ abonnement2.anom }}</td>
                <td>{{ abonnement2.idu }}</td>
                <td>{{ abonnement2.ida }}</td>
                <td>{{ abonnement2.noma }}</td>
                <td>
                    <a class="btn btn-outline-primary" style="width: 71px" href="{{ path('sb', {'id': abonnement2.id}) }}">voir</a>
                    {{ include('abonnement2/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
